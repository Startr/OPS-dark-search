[Unit]
Description=microsocks SOCKS5 (loopback only)
After=network-online.target

[Service]
ExecStart=/usr/bin/microsocks -i 127.0.0.1 -p 1080
DynamicUser=yes
Restart=always

[Install]
WantedBy=multi-user.target
